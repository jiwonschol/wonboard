# 사용자 사전 및 한국어 검사 검증 — 2026-09-12

## 183 공통 문법 보완 후 실제 macOS 재검증

최신 공통 검사기와 로컬 macOS 빌드를 새 임시 프로필 `/tmp/wonboard-native-grammar-183-n1eXUd`로 실행했다. 한 것임에/교육받았어요/할 뿐만 아니라/떠먹여 준다 후보 선택·수정·undo/redo·저장, 추천하고픈/오고파요 보존을 확인했다. 갈축 등록 후 갈축이나의 조사 결합과 주변 쓸 때 수정이 유지됐다. 앱 프로세스를 실제 종료하고 다시 실행한 뒤 문서·사전 유지, 등록어 삭제 후 후보 없는 인터넷 표현 검토 복원이 통과했다. `/tmp/wonboard-native-grammar-result-183.json`의 passed=true, platform=darwin, URL=wonboard://app/를 확인했고 화면도 직접 열었다. 재현 스크립트와 결과·화면은 Git 제외 `local-corpora/grammar-183/evidence/`에도 보존한다.

이번 변경은 저장 계층과 UI를 바꾸지 않았다. 개인 사전은 브라우저 origin/Electron 프로필의 기존 설정 저장이며 영어 대소문자 구별을 유지한다. 계정·기기 동기화나 공개 사이트 전송을 추가하지 않았다. 웹 회귀와 새 게시글의 언어 품질 결과는 `spelling-progress.md`183을 따른다. Windows 실제 실행·실제 OS 한글 조합 입력·원래 독립 언어 품질 게이트는 미완료다. 아래163 기록은 당시 전체 UI 실패까지 포함한 과거 결과이며 이번 맞춤법 범위의 검증과 구별한다.

## 현재163 누적 검증 결과

Vitest148개(11파일), 웹 빌드, macOS 빌드가 종료 코드0으로 통과했다. 실제 macOS 격리 프로필에서 사전 등록·삭제, 닉네임 등록 후 호칭 공백 유지, 수정·undo/redo·저장, 앱 종료/재실행 후 문서·사전 유지가 통과했다. `/tmp/wonboard-native-dictionary-result-163.json`의 passed:true·platform:darwin을 확인했고 `/tmp/wonboard-native-dictionary-wxcfCX/verified.png` 화면도 직접 확인했다. 재현 스크립트 `/tmp/wonboard-native-dictionary-check-163.mjs`다. 실제 OS IME/Windows 증거가 아니다.

전체 Chrome149개는147통과·2실패·8.5분·종료 코드1로 끝났다(`/tmp/wonboard-final-163-full-e2e.log`). 맞춤법 파일의120개는 전부 통과했다. 실패1은 별도 미커밋 공백 정리 시험이 찾는 Tidy whitespace 버튼이 현재 WritingToolbar에 연결되어 있지 않은 상태이며, 사용자 공백 작업은 수정하지 않았다. 실패2는 서식 제거 시험에서 부분 선택 Hello 대신 Hello world가 남은 것이다. 단독 재시험도 같은 지점에서 실패했다(`/tmp/wonboard-final-163-format-retry.log`). 전체 UI 회귀가 녹색이라고 주장하지 않는다. 두 실패는 별도 편집기 작업으로 기록하며, 검사기·형태 분석기·공통 사전의163 스냅샷 바이트 일치를 확인했다.

웹 Worker는 review.worker-DHw0lI8v.js,6,197,005바이트·gzip1,724,331바이트·SHA256 c3ae4de614929bd272344043a5ee0a6099a69b275c7d1d210934e20431bc91f8다. `local-corpora/worker-asset-163.json`은 Git 제외를 확인했다. 크기만 측정했으며 새 실행 속도·메모리 주장이 아니다.


전체 품질 목표는 미완료다. 이번 변경은 개인 사전의 저장·검사 상태 경계와 제한된 한국어 인식을 보완한다. 기존 UI와 단축키 방향을 유지했으며 기존 미커밋 공백 정리 작업을 보존했다.

## 최신 닉네임 및 전체 회귀 검증156

닉네임만 등록해 호칭 공백까지 숨기는153–154 실험은 철회했다.156에서는 모험러 미등록 범위와 님 앞 공백 후보를 분리했다. 실제 Chrome에서 기본어 등록 후 공백 후보 유지·수정·undo/redo·저장·재열기·삭제 후 검토 복원을 통과했다. 스펙 시험과 기존 교정 시험을 포함한 집중3개16.6초·Node298도 통과했다. 모험러님 전체 등록은 해당 의도 표현을 보존한다. 후속으로 현재156 macOS 빌드의 격리 프로필에서도 실제 종료/재실행 후 닉네임·문서 유지와 삭제 복원을 통과했다(`/tmp/wonboard-native-dictionary-result-156.json`). 현재156 전체 Chrome119개6.8분·Vitest148·웹/데스크톱 빌드도 종료 코드0으로 통과했다. Windows와 실제 OS IME는 미검증이다.

## 실제 OS IME 대조 시도: 개발146

CUA 네이티브 Chrome 키 입력 g/k/s가 웹 입력란에서 composition 이벤트를 발생시키지 않았다. 원보드와 순수 HTML 입력란 모두 ㅎㅏㄴ으로 들어갔고 대조 입력란은 compositionstart/update/end 각각0회, 일반 input3회·isComposing:false였다. 주소창에서는 한으로 합성됐다. 이는 해당 자동화 경로의 검증 한계이며 제품의 실제 OS IME 통과나 결함을 의미하지 않는다. 기존 합성 이벤트 시험과 구분해 실제 OS IME 게이트를 미검증으로 유지한다.

## 최신 후속 검증: 개발141

최신 웹·데스크톱 빌드 및 Node289·Vitest148·타입·데이터 정책 검사를 통과했다. macOS 앱을 새 임시 프로필에서 실행해 업글·자모 표현의 등록, 인접 됬어요 수정, undo/redo, 문서 저장, 앱 종료·재실행 후 다른 활용형 인식, 삭제 후 검토 복원을 확인했다. 결과 `/tmp/wonboard-native-dictionary-result-141.json`, 재현 `/tmp/wonboard-native-dictionary-check-141.mjs`다. 브라우저 집중2개도 통과했으며 전체 Chrome118개6.6분·종료 코드0도 통과했다. 화면 캡처에서 기존 단축키와 개인 사전 표시를 확인했다. Windows와 실제 OS IME는 미검증이며 언어 품질의 독립 완료 기준도 미충족이다.

## 이전 후속 검증: 개발031

Chrome전체61개와 최신 macOS 빌드의 격리 프로필 재실행 시험이 통과했다. 추가·중복·수정·undo/redo·저장·종료/재실행·삭제 후 검토 복원을 확인했다. 상세 프로필과 결과 파일은 아래 개발031 기록에 남겼다. Windows 및 실제 OS 한글 조합 입력은 미검증이다.

## 이전 후속 검증: 개발025

동작 약어 업글을 등록하면 검증된 하다 활용에서도 해당 표현만 인식하고 다른 단어의 철자·띄어쓰기 검사는 유지한다. 임의의 개인 명사에 이 파생을 일반화하지 않았다. Chrome에서 등록·인접 수정·undo/redo·저장·재열기·다른 활용·삭제를 포함한53개 시험이 통과했다. macOS 개발 앱에서도 실제 종료·재실행 후 다른 활용 인식과 삭제를 확인했다. 첫 macOS 시험의 일반 미등록 기대 문구는 실제 인터넷 표현 검토와 달라 실패했으며, 문구만 바로잡은 전체 재실행에서 통과했다. Node203·타입·정책·빌드 통과. Windows/OS IME와 독립 언어 품질 목표는 미완료다.

## 이전 후속 검증: 개발023

같은 새 버전 창의 사전 변경은 origin의 동일 잠금 안에서 읽고 쓴다. 실제 Chrome 두 탭의 등록 두 건과 등록/삭제가 함께 대기한 뒤 모두 보존되고, 창을 닫아 취소한 대기 저장은 재열기 후에도 나타나지 않았다. 전체51개·Node201·Vitest148·타입·정책·웹/데스크톱 빌드가 통과했다. macOS 개발 앱 재실행·삭제도 `/tmp/wonboard-native-dictionary-result-023.json`에서 통과했다.

저장 대기 중 조작을 막고 기존 검사 응답을 무효화한다. 저장 잠금을 제공하지 않는 환경에서는 변경을 거부하고 기존 목록을 보존한다. 구버전 창이나 개발자 도구에서 잠금 없이 직접 쓴 데이터는 이 조정 범위에 들지 않는다. 저장 방식은 기존 localStorage를 유지하며 기기 간 동기화는 없다. Windows·실제 OS IME·전체 언어 품질 목표는 미완료다.

## 이전 후속 검증: 개발022

Node201·Vitest147·Chrome전체50·타입·정책·웹/데스크톱 빌드가 통과했다. 숫자 표현에서도 개인 등록 전체형은 유지하되 단위 등록으로 인접 오류까지 숨기지 않는다. 현재 macOS 개발 앱의 사전·문서 저장과 실제 재실행·삭제 흐름은 `/tmp/wonboard-native-dictionary-result-022.json`에서 통과했다. Windows·OS 한글 IME·동시 저장 원자성 및 전체 품질 목표는 미완료다.

## 이전 후속 검증: 개발021

Node200·Chrome전체49·타입·정책·웹/데스크톱 빌드가 통과했다. 새 짧은 활용·중첩 의존 명사 후보에도 전체 표현 개인 등록 우선권이 유지되는 단위 시험을 추가했고, 기존 사전 UI 회귀와 macOS 개발 앱의 실제 재실행·등록 삭제 흐름도 통과했다(`/tmp/wonboard-final-021-e2e.log`, `/tmp/wonboard-native-dictionary-result-021.json`). 링크·코드 전용 macOS 보존과 Vitest147은020 실행 기록이다. 전체 품질 목표 및 Windows/OS IME 완료를 주장하지 않는다.

## 이전 후속 검증: 개발020

Node199/Vitest147/타입·정책·빌드와 Chrome전체48개가 통과했다. 개인 사전 회귀 시험을 포함하며 실제 링크/코드 서식 뒤 공백과 인접 철자를 독립 수정하는 흐름을 추가했다. href와 보호 내용·서식은 undo/redo 및 저장·새로고침 후 유지됐고, macOS 개발 앱도 실제 재실행 후 이를 보존했다. `/tmp/wonboard-final-020-e2e.log`, `/tmp/wonboard-native-marked-result-020.json`. 링크 내용을 사용자 사전으로 등록하거나 Worker 입력에 노출하지 않았다. 전체 품질·Windows·OS IME 완료를 뜻하지 않는다.

## 이전 후속 검증: 개발019

현재 공통 검사기로 Node198·Chrome전체46·타입·데이터 정책·웹/데스크톱 빌드가 통과했다. macOS 개발 앱 사전·조사·인접 오류·undo/redo·저장·실제 재실행·삭제도 격리 프로필에서 통과했다(`/tmp/wonboard-native-dictionary-result-019.json`). 일반 텍스트의 보호 구간 뒤 공백 수정은 개인 단어를 등록해도 독립적으로 남는다. 실제 링크/코드 서식 뒤 경계는 아직 다음 구현이며 이 시험으로 증명하지 않는다. Windows/OS IME와 동시 저장 원자성 한계는 유지한다.

## 이전 후속 검증: 개발018

설치 Chrome 전체45개가1.3분에 통과했다.017에서 바로잡은 좁은 화면 재열기 시험과 추가 관형형 후보의 선택·수정·undo/redo·저장을 포함한다. Node196, 타입·데이터 정책·웹/데스크톱 빌드 통과.018 macOS 개발 앱도 격리 프로필에서 사전·조사·인접 오류·저장·실제 재실행·삭제를 통과했다. `/tmp/wonboard-final-018-e2e.log`, `/tmp/wonboard-native-dictionary-result-018.json`. Windows/OS IME 및 동시 저장 원자성 한계는 그대로다.

## 이전 후속 검증: 개발017

인터넷 표현 검토를 일반 미등록 안내와 구분했다. `컴` 기본 단어 등록 후 `컴으로`를 유지하고 `비추입니다`는 이번만 건너뛴 뒤 인접 철자를 수정했다. 저장·새로고침에서 컴 등록은 유지되고 비추 검토는 돌아왔다. 컴 삭제 후 다시 검토가 표시되는 한국어390px Chrome 흐름을 통과했다. 새로고침 후 기존 설정 패널을 닫고 도구에 진입했다. `/tmp/wonboard-community-reload-e2e.log`, `/private/tmp/wonboard-community-review-ko-chromium.png`.

Node194/Vitest146/타입·정책/웹·데스크톱 빌드 통과. 전체 Chrome43개 통과/새 시험1개 실패 뒤 그 시험의 화면 진입을 바로잡아 해당1개 통과이며, 수정 후 전체44개 재실행을 주장하지 않는다. macOS 개발017에서도 별도 프로필로 사전·문서 저장·재실행·삭제 흐름이 통과했다(`/tmp/wonboard-native-dictionary-result-017.json`). Windows/OS IME 미검증과 동시 저장 원자성 한계는 유지한다.

## 이전 후속 검증: 개발016

Node190/Vitest146/타입·정책/웹·데스크톱 빌드와 설치 Chrome43개가 통과했다. 복수 `들` 및 `만의/보단`이 붙은 미등록 표현에서 기본 단어를 제시하고, 등록·재검사·인접 수정·저장·삭제를 실제 UI로 확인했다. 이름을 한 번 건너뛴 뒤 바로 뒤 서술격 공백을 고치면 같은 이름이 다시 뜨던 실패도 재현하고 수정했다. `/tmp/wonboard-final-016-*.log`.

현재 macOS 개발 빌드를 임시 프로필에서 실제 실행했다. `아즈휼들만의`를 기본 단어 등록으로 인식하고 인접 철자·이모티콘을 처리한 뒤 undo/redo, 저장, 앱 종료·재실행, 삭제 후 미등록 복귀를 확인했다. `/tmp/wonboard-native-dictionary-result-016.json`. Windows 및 실제 OS 한글 조합은 아직 미검증이다. 아래 초기 실행 기록의 macOS 미검증 표시는 당시 상태이며 최신 결과와 구분한다.

## 작업 위치와 공통 구현

- 정식 경로: `/Users/jiwon/develop/projects/wonboard`; origin `https://github.com/jiwonschol/wonboard.git`; branch `codex/desktop-local-storage`; 시작 HEAD `19ef8f2`.
- 별도 기존 Claude worktree 2개는 변경하지 않았다. 저장소 내부 AGENTS.md는 없고 상위 공통 규칙 및 work-discipline을 확인했다.
- 웹 `apps/client`, Windows/macOS `apps/desktop`은 동일한 App과 `packages/editor`를 사용한다. 운영체제별 검사기 복제 없음.
- 개인 사전은 기존 localStorage 키와 각 브라우저/Electron 프로필에 유지된다. 문서 저장소(웹 IndexedDB, 데스크톱 SQLite, Sites 문서 API)와 분리한다. 기기·계정 동기화 구현 없음. 기존 기본 사전, 자체 통용어 목록, 개인 사전의 소유를 분리했다.
- 사전 참조를 검색한 결과 개인 사전은 편집기와 Worker 사이에서만 사용된다. 문서 모델·renderer·이미지 URL·Sites 전송에 개인 사전을 추가하지 않았다. 네트워크 없이 동작하는 공통 검사기 계약을 유지한다.

## 변경과 확인

- 저장 직전 최신 목록을 읽는 추가/삭제 함수, NFC 및 아포스트로피 동등 중복 처리, 실패 시 기존 저장값 보존. 영어 대소문자 구별은 기존 동작 그대로다. 대문자 약어와 camelCase 미등록어에 임의 후보를 강요하지 않는다.
- 목록에서 직접 추가 가능, 중복 안내, 항목별 삭제, 재시작 후 유지. 같은 창 알림과 다른 창 storage 이벤트에서 재검사하고 과거 Worker 응답을 무효화한다. 초기 구현은 최신 읽기만 적용했으나 개발023부터 같은 버전 창의 읽기·쓰기를 Web Locks로 직렬화한다. 직접 쓰는 구버전 창은 이 조정 대상이 아니다.
- 조합 중 Enter/등록 보호, 사전 입력 중 늦은 결과의 포커스 이동 방지. 실제 OS 한글 IME 검증은 별개이며 이번 시험의 composition 이벤트는 합성 입력이다.
- 등록어+조사는 인정하면서 주변 띄어쓰기와 다른 오타를 유지한다. 이번만 건너뛰기, 검사 전체의 동일 표면형 건너뛰기, 영구 등록을 따로 시험했다.
- 삭제당하고의 잘못된 분절과 강등당했었네요의 미등록 안내를 제한된 피동 접미사 결합으로 수정했다. 질게 질문 등 명시적 게시판 단서는 모호한 미등록 검토로 표시하고 죽을 질게 끓였다의 정상 활용은 보존한다. 근거와 제한은 spelling-orthography-sources.md에 있다.

## 실행 결과

- Vitest 145개, Node 154개 통과. 타입 검사 및 현재 사전 데이터·고지의 MIT/Apache 정책 검사 통과.
- Chromium 맞춤법 전체 첫 실행 34개 통과. 전체 편집기 첫 실행 62개 통과 / 별도 공백 정리 1개 실패. `tests/e2e/whitespace.spec.ts:11`에서 아직 연결되지 않은 `Tidy whitespace` 버튼을 기다리다 45초 초과했다. 별도 사용자 작업이므로 변경하지 않았다.
- 최종 변경 후 Chromium/Firefox/WebKit에서 사전·단축키·본문 변경 시험 30개 통과(44.5초). 이전 검사 응답을 시험에서 실제로 보류했다가 사전 변경 후 전달하여 결과가 되살아나지 않는 것을 확인했다. 추가 시험이므로 이전 실행 개수와 합산해 정확도 수치로 쓰지 않는다.
- 웹 및 공통 데스크톱 빌드 통과. 기존 큰 chunk 경고는 남는다. 실제 Windows/macOS 앱 실행·앱 재시작 및 물리 IME는 미검증이다.
- `cua_repl`로 살아 있는 5173 편집기에 검증 문서를 새로 만들었다. 등록→조사 인식→주변 띄어쓰기 수정→Enter 건너뛰기→Shift+Enter 교정→undo/redo→저장 완료→새로고침→사전 유지→중복 안내→삭제 후 미등록 복원을 실제 조작으로 확인했다. 시험 등록어는 삭제했고 검증 문서는 로컬에 남았다. 마지막 화면 확인 시 로그인 화면으로 전환되어 있었으며 이전 저장/재열기 증거와 구분한다.
- 첫 Playwright 실행은 Chromium 실행 파일 부재가 아니라 macOS 샌드박스의 Mach 포트 권한 오류였다. 허용된 로컬 시험 실행으로 재시도해 통과했다. 컴퓨터 사용 도구 자체는 정상 호출됐다.
- 브라우저 Worker 1만 자 warm 5회씩: 정상 최대18.6ms, 오류452.8ms, 합성 미등록172.3ms, 부사 경계67.8ms, 64자 미등록258.8ms, 영어 오류54.3ms. cold 약977.9ms. 이번 환경의 시험 입력 결과이며 모든 장문의 지연 보장이 아니다. 사전 두 파일 gzip 합계1,688,616바이트.

## 독립 평가와 남은 작업

기존 개발 글70개(제목/본문140필드)를 이번 변경 전후 비교했다. 철자87·띄어쓰기372·미등록617로 출력 변화0필드다. 새로 고친 사용자 피드백 사례와 이 개발 자료의 결과를 혼용하지 않는다.

`compiled-provisional-240.json`을 재확인했다. 총4,206행, 미판정647행, 정상 전체 문장 후보326개, 최종 판정0행, 예측 미실행이다. 최종 평가 진입점은 실제로 `Unfinished annotations cannot be evaluated`를 반환했다. 주석을 자동 확정하거나 쉬운 행을 따로 골라 독립 정확도로 발표하지 않았다. **독립 정확도 평가는 완료하지 못했으며, 자료의 자격 검증까지 수행했다.**

재진입 자료는 Git에서 제외된 `local-corpora/holdouts/candidate-2026-09-12-dictionary/qualification.json`과 그 폴더의 동결 코드/데이터다. 개인 원문을 포함한 개발 비교는 `local-corpora/reports/dictionary-development-2026-09-12.json`에만 있다. 원본 보류 파일은 변경하지 않았다.

다음 필수 작업은 미판정647행 및 전체 문장 판정의 근거·모호성 검토, 최종 자료의 중복·노출 감사, 동결 후보 독립 평가다. 첫 후보98% / 상위3후보95% / 정상 문장 오제안1% 이하 / 오류200구간·정상200문장 / 미등록95%의 기존 기준은 미충족 상태다. 뷰는·하락장입니다·시작했으니까요·잘되고·매수타이밍 같은 남은 미등록 안내, 더 넓은 문맥과 어휘 누락도 해결로 표시하지 않는다.

원격 push·PR·merge·배포·유료 소비·새 의존성·DB 마이그레이션 없음.


## 후속 검증: 인터넷 감정 표현과 인접 오류

클리앙 예측 대조에서 자모가 섞인 어절 전체를 미등록으로 보내 앞 단어의 철자 오류가 가려지는 문제를 발견했다. 문장 좌표를 유지하면서 한글 단어와 자모 표현을 분리한다. `됬어요ㅋㅋㅋ`는 `됐어요` 후보와 `ㅋㅋㅋ` 검토를 각각 제공한다. `ㅋㅋㅋ` 등록이 앞 단어의 오류를 숨기지 않는다. 전체 혼합 표현을 의도적으로 정확히 등록한 경우는 그 개인 항목의 기존 범위를 유지한다.

개인 사전에 `ㅠ_ㅠ` 같은 자모 사이 단일 밑줄 감정 표현을 허용했다. URL·공백 문장·일반 식별자·임의 밑줄 조합은 여전히 등록 대상이 아니다. 같은 표현 추가/중복/삭제를 저장 계층에서 시험했다. 한 글자 영어 표기 `B/C/f`는 다른 알파벳으로 바꾸라는 근접 후보를 내지 않고 미등록 검토로 남긴다. 대소문자 정책은 바꾸지 않았다.

Node174, Vitest146 및 타입 검사는 통과했다. 새 Chrome 등록·저장·재열기·삭제 시험은 `/tmp/wonboard-emoticon-e2e.log`에 실행 결과를 남긴다. 이전 실기기 미검증 제한은 그대로다.

최신 Chrome 실행: `/tmp/wonboard-emoticon-e2e.log`에서38개 전체 통과(약1분). 감정 표현 등록 후에도 인접 철자 수정 후보가 유지되고, 저장·새로고침 후 등록 유지와 삭제 후 미등록 복귀를 확인했다.


## 실제 macOS 앱 재시작 검증

공통 데스크톱 빌드를 실제 Electron/macOS에서 실행했다. `app.getPath('userData')`가 임시 폴더인지 확인한 후만 조작하여 기존 사용자 저장 공간을 보존했다. `아즈휼/ㅋㅋㅋ/ㅠ_ㅠ`를 UI로 등록하고 `아즈휼도` 인식과 인접 철자 오류 교정을 확인했다. undo/redo와 저장 후 앱 프로세스를 종료하고 다시 실행해 문서 및 사전 유지, 기본 단어 삭제 후 미등록 복귀를 확인했다. 보고서 `/tmp/wonboard-native-dictionary-result.json`, 화면 `/tmp/wonboard-native-dictionary-7RwRaI/verified.png`. 이는 실제 개발 빌드 앱 실행 증거이며 서명·설치된 배포 앱 검증으로 부풀리지 않는다. Windows와 물리 한글 IME는 아직 별도다.

### 영어 보완 정책 — 개발028

개인 사전의 대소문자 구분은 유지한다. 공통 보완 목록에서 확립된 기술 약어 CPU/GPU는 대소문자를 고치지 않고 인식한다. 이를 미등록 약어 전체에 적용하지 않으며 ahk→AHK 후보와 임의 개인 단어의 대소문자 차이는 기존 정책대로 시험했다. 개발028 Chrome전체58개에 원문 gpu/cpu/GPU 보존과 인접 영어 오타 수정·저장이 포함된다.
## 개발031 macOS 재실행 검증

최신 데스크톱 빌드에서 별도 프로필 `/tmp/wonboard-native-dictionary-2HccBq`로 개인 사전 추가·중복 표시·옆 오류 수정·undo/redo·저장·실제 앱 종료 후 재실행·삭제 후 검토 복원을 다시 확인했다. `/tmp/wonboard-native-dictionary-result-031.json`은 passed=true, 실제 URL은 wonboard://app/, 플랫폼은 darwin이다. 기존 사용자의 프로필은 사용하지 않았다. Windows 실행과 실제 OS 한글 조합 입력은 이 시험의 증거 범위 밖이다.
